/**
 * NEXUS UI — TOKENS · index.js
 * Punto de entrada único para todos los tokens.
 *
 * Uso en cualquier componente:
 *   import { TOKENS, COLOR_MAP, GLOWS, RADIUS, TRANSITIONS, TYPOGRAPHY } from "@/design-system/tokens";
 *   import { injectGlobalStyles }                                         from "@/design-system/tokens";
 */

export { TOKENS, COLOR_MAP }        from "./colors";
export { TYPOGRAPHY }               from "./typography";
export { GLOWS, RADIUS, TRANSITIONS } from "./effects";
export { injectGlobalStyles }       from "./styles";
